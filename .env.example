# ==============================================================================
# CryptoForensics - Environment Configuration Template
# ==============================================================================
# No private API keys or secret credentials are required.
# The application runs on authentic local dataset files and offline ML models.

# Backend Configuration
HOST=0.0.0.0
PORT=8000

# Optional: Custom path overrides for dataset and models if mounted externally
# DATASET_DIR=dataset/elliptic_bitcoin_dataset
# MODELS_DIR=models

# Frontend Configuration (for production deployment proxy / rewrite)
# VITE_API_BASE_URL=http://localhost:8000
