{
  "name": "crypto-forensics",
  "version": "1.0.0",
  "private": true,
  "description": "CryptoForensics AML & Blockchain Investigation Intelligence Platform",
  "scripts": {
    "dev": "npm run --prefix frontend dev",
    "build": "npm run --prefix frontend build && node -e \"const fs = require('fs'); try { fs.cpSync('frontend/dist', 'dist', {recursive: true}); } catch(e){}\"",
    "preview": "npm run --prefix frontend preview"
  },
  "devDependencies": {
    "vite": "^5.1.6"
  }
}
